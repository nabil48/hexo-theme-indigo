<% if (page.total > 1){ %>
<nav id="page-nav">
    <div class="inner">
    <%- paginator({
        prev_text: __(paginator.prev),
        next_text: __(paginator.next)
    }) %>
    </div>
</nav>
<% } %>